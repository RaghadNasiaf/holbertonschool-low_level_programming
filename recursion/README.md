Recursion Project

This directory contains recursion helper functions implemented without using the C standard library (except _putchar when allowed).
Each function follows Betty style and is compiled on Ubuntu 20.04 with:
gcc -Wall -Werror -Wextra -pedantic -std=gnu89
